# B2b — iOS 실 UI (디바이스목록 + 터미널) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** iPad에서 서버 URL·SSH 키를 넣고 디바이스 목록에서 노드를 골라 실제 SSH 셸을 여는 iOS MVP를 B2a 기반 위에 구현. macOS 회귀 0.

**Architecture:** iOS 자격은 `TerminalSession.defaultCredentials()`의 새 iOS 분기가 Keychain PEM + 설정 username으로 공급(기존 Store→Session→CitadelSession→다중키→TOFU 흐름 재사용). 터미널 뷰는 mac `SwiftTermRepresentable`(NSViewRepresentable)을 iOS `UIViewRepresentable`로 미러링. SwiftUI 화면(설정/키임포트/디바이스목록/터미널)은 새 `HydraiOS/` 파일로만 추가.

**Tech Stack:** SwiftUI(iOS 17), Swift 5 모드, SwiftTerm iOS `TerminalView`, Keychain(`CredentialStore`), `APIClient`(REST), CitadelSession(SSH), xcodegen/xcodebuild.

## Global Constraints
- **macOS 회귀 0**: `TerminalSession.defaultCredentials()`의 `#if os(macOS)` 분기는 **byte-identical** 유지. 매 관련 단계 `cd Hydra && swift test`(77 tests/1 skip 기대) + 필요 시 `make hydra-app`.
- **불변 파일**: mac `Views/**`, `SwiftTermRepresentable`(mac), `Package.swift`, `Makefile`, `CitadelSession`/`SSHSession`, A의 다중키 루프(`connect`).
- 새 iOS 뷰는 `Hydra/HydraiOS/` 아래에만. `project.yml`의 HydraiOS `sources`는 `HydraiOS`를 통째로 glob하므로 하위 파일은 자동 포함(별도 project.yml 수정 불필요; 새 하위 디렉토리 추가 시 재확인).
- iOS 검증: 각 UI 태스크 후 `xcodegen generate && xcodebuild -project Hydra.xcodeproj -scheme HydraiOS -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO` → `** BUILD SUCCEEDED **`.
- 무암호 ed25519 키 전제(패스프레이즈 프롬프트는 범위 밖). 스터빙/`fatalError` 금지.

---

### Task 1: iOS 자격 배선 (Keychain 키 + defaultCredentials iOS 분기)

**Files:**
- Modify: `Hydra/Hydra/Services/CredentialStore.swift`, `Hydra/Hydra/Services/TerminalSession.swift`
- Test: `Hydra/Tests/HydraTests/IOSCredentialsTests.swift` (create)

**Interfaces:**
- Produces: `CredentialStore.Key.sshPrivateKeyPEM`; `TerminalSession.makeImportedCredentials(pem:user:port:) -> SSHCredentials`(플랫폼 무관, 테스트 가능); iOS `defaultCredentials()`가 이를 사용.

- [ ] **Step 1: 실패 테스트 작성** — `Hydra/Tests/HydraTests/IOSCredentialsTests.swift`

```swift
#if os(macOS)
import XCTest
@testable import Hydra

/// The iOS credential builder is a platform-agnostic pure function so it can be
/// unit-tested on macOS even though defaultCredentials() only calls it on iOS.
final class IOSCredentialsTests: XCTestCase {
    func testBuildsSingleImportedKey() {
        let creds = TerminalSession.makeImportedCredentials(pem: "PEMDATA", user: "dave", port: 22)
        XCTAssertEqual(creds.user, "dave")
        XCTAssertEqual(creds.port, 22)
        XCTAssertEqual(creds.keys.count, 1)
        XCTAssertEqual(creds.keys.first?.algorithm, "imported")
        XCTAssertEqual(creds.keys.first?.pem, Data("PEMDATA".utf8))
    }
    func testEmptyPemYieldsNoKeys() {
        let creds = TerminalSession.makeImportedCredentials(pem: "", user: "root", port: 22)
        XCTAssertEqual(creds.user, "root")
        XCTAssertTrue(creds.keys.isEmpty)
    }
}
#endif
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd /Users/dave/iWorks/hydra/Hydra && swift test --filter IOSCredentialsTests 2>&1 | tail -12`
Expected: 컴파일 실패 (`makeImportedCredentials` 미정의).

- [ ] **Step 3: CredentialStore에 키 추가** — `CredentialStore.swift`의 `enum Key` 안에 추가:

```swift
        case deviceUUID = "device_uuid"
        case sshPrivateKeyPEM = "ssh_private_key_pem"
```

- [ ] **Step 4: TerminalSession에 헬퍼 + iOS 분기 추가**

(4a) `defaultCredentials()` 아래(같은 타입 내)에 플랫폼 무관 헬퍼 추가:
```swift
    /// Builds credentials from a single imported private key (iOS path). Pure and
    /// platform-agnostic so it is unit-testable on macOS.
    nonisolated static func makeImportedCredentials(pem: String, user: String, port: Int) -> SSHCredentials {
        let trimmed = pem.trimmingCharacters(in: .whitespacesAndNewlines)
        guard !trimmed.isEmpty else { return SSHCredentials(user: user, port: port, keys: []) }
        return SSHCredentials(user: user, port: port,
                              keys: [ResolvedKey(path: "keychain", pem: Data(trimmed.utf8), algorithm: "imported")])
    }
```

(4b) `defaultCredentials()`의 `#else`(iOS) 분기를 아래로 교체(현재 iOS 분기는 `CitadelSession()` 백엔드 선택과 무관 — 여기선 자격 해석):
> 현재 `defaultCredentials()`는 `#if os(macOS)` … `#else` … 없이 macOS 로직만 있고 파일은 un-gate된 상태다. iOS에서도 이 함수가 컴파일되므로, **함수 본문을 플랫폼 분기**로 감싼다:
```swift
    static func defaultCredentials() -> SSHCredentials {
        #if os(macOS)
        // ── 기존 macOS 로직 그대로 (config.yaml + SSHKeyLocator.orderedKeyPairs) ──
        let user: String
        let port: Int
        var paths: [String] = []
        if let r = ClusterSSHConfig.load() {
            user = r.user; port = r.port
            paths.append(r.privateKeyPath)
        } else {
            user = NSUserName(); port = 22
        }
        if let pairs = try? SSHKeyLocator.orderedKeyPairs() {
            for kp in pairs where !paths.contains(kp.privatePath) {
                paths.append(kp.privatePath)
            }
        }
        let keys: [ResolvedKey] = paths.compactMap { p in
            guard let pem = FileManager.default.contents(atPath: p) else { return nil }
            let base = (p as NSString).lastPathComponent
            return ResolvedKey(path: p, pem: pem,
                               algorithm: SSHKeyLocator.algorithmName(forBasename: base))
        }
        return SSHCredentials(user: user, port: port, keys: keys)
        #else
        // ── iOS: 임포트한 키(Keychain) + 설정 username ──
        let pem = CredentialStore.shared.get(.sshPrivateKeyPEM)
        let user = UserDefaults.standard.string(forKey: "sshUsername") ?? "root"
        return makeImportedCredentials(pem: pem, user: user, port: 22)
        #endif
    }
```
> **주의**: macOS 분기는 현재 본문과 byte-identical이어야 한다(줄바꿈/로직 불변). 실제 현재 본문을 그대로 `#if os(macOS)` 안으로 옮길 것.

- [ ] **Step 5: 테스트 통과 + macOS 회귀**

Run: `cd /Users/dave/iWorks/hydra/Hydra && swift test 2>&1 | tail -6`
Expected: 전체 PASS (77 + 2 신규 = 79, 1 skip). macOS 자격 로직 불변.

- [ ] **Step 6: iOS 컴파일 확인**

Run: `cd /Users/dave/iWorks/hydra/Hydra && xcodegen generate >/dev/null && xcodebuild -project Hydra.xcodeproj -scheme HydraiOS -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -6`
Expected: `** BUILD SUCCEEDED **` (iOS 분기 컴파일).

- [ ] **Step 7: 커밋**

```bash
cd /Users/dave/iWorks/hydra
git add Hydra/Hydra/Services/CredentialStore.swift Hydra/Hydra/Services/TerminalSession.swift Hydra/Tests/HydraTests/IOSCredentialsTests.swift
git commit -m "feat(ios): iOS SSH credentials from Keychain (defaultCredentials iOS branch)

Add CredentialStore.sshPrivateKeyPEM and a platform-agnostic
makeImportedCredentials() helper; defaultCredentials() gains an iOS branch that
builds a single imported key from the Keychain + sshUsername setting, reusing
A's multi-key loop. macOS branch unchanged."
```

---

### Task 2: 설정 · 키 임포트 · 디바이스 목록 화면 (SwiftUI)

**Files:**
- Create: `Hydra/HydraiOS/Screens/SettingsScreen.swift`, `Hydra/HydraiOS/Screens/KeyImportScreen.swift`, `Hydra/HydraiOS/Screens/DeviceListScreen.swift`

**Interfaces:**
- Consumes: `CredentialStore`, `APIClient`, `Device`, `UserDefaults`.
- Produces: 세 SwiftUI `View`. `DeviceListScreen`은 행 탭 시 `onSelect: (Device) -> Void` 클로저 호출.

- [ ] **Step 1: KeyImportScreen** — `Hydra/HydraiOS/Screens/KeyImportScreen.swift`

```swift
import SwiftUI
import UniformTypeIdentifiers

struct KeyImportScreen: View {
    @State private var pem: String = CredentialStore.shared.get(.sshPrivateKeyPEM)
    @State private var showingImporter = false
    @State private var message: String?

    private var hasKey: Bool { !CredentialStore.shared.get(.sshPrivateKeyPEM).isEmpty }

    var body: some View {
        Form {
            Section("SSH 개인키 (PEM)") {
                TextEditor(text: $pem)
                    .font(.system(.footnote, design: .monospaced))
                    .frame(minHeight: 160)
                Button("Files에서 가져오기") { showingImporter = true }
            }
            Section {
                Button("저장") { save() }
                if hasKey {
                    Button("삭제", role: .destructive) {
                        CredentialStore.shared.set(.sshPrivateKeyPEM, value: "")
                        pem = ""; message = "삭제됨"
                    }
                }
            }
            if let message { Section { Text(message).foregroundStyle(.secondary) } }
            Section {
                Text(hasKey ? "키 저장됨 ✓" : "저장된 키 없음")
                    .foregroundStyle(hasKey ? .green : .secondary)
            }
        }
        .navigationTitle("SSH 키")
        .fileImporter(isPresented: $showingImporter,
                      allowedContentTypes: [.data, .text, UTType.item],
                      allowsMultipleSelection: false) { result in
            switch result {
            case .success(let urls):
                guard let url = urls.first else { return }
                let ok = url.startAccessingSecurityScopedResource()
                defer { if ok { url.stopAccessingSecurityScopedResource() } }
                if let data = try? Data(contentsOf: url), let text = String(data: data, encoding: .utf8) {
                    pem = text
                } else { message = "파일을 읽을 수 없습니다" }
            case .failure(let e): message = "가져오기 실패: \(e.localizedDescription)"
            }
        }
    }

    private func save() {
        let trimmed = pem.trimmingCharacters(in: .whitespacesAndNewlines)
        guard trimmed.contains("PRIVATE KEY-----") else {
            message = "PEM 개인키 형식이 아닙니다 (-----BEGIN ... PRIVATE KEY----- 필요)"
            return
        }
        CredentialStore.shared.set(.sshPrivateKeyPEM, value: trimmed)
        message = "저장됨 ✓"
    }
}
```

- [ ] **Step 2: SettingsScreen** — `Hydra/HydraiOS/Screens/SettingsScreen.swift`

```swift
import SwiftUI

struct SettingsScreen: View {
    @AppStorage("serverURL") private var serverURL: String = "http://localhost:8080"
    @AppStorage("sshUsername") private var sshUsername: String = "root"

    var body: some View {
        Form {
            Section("서버") {
                TextField("http://<host>:8080", text: $serverURL)
                    .textInputAutocapitalization(.never)
                    .autocorrectionDisabled()
                    .keyboardType(.URL)
            }
            Section("SSH") {
                TextField("username", text: $sshUsername)
                    .textInputAutocapitalization(.never)
                    .autocorrectionDisabled()
                NavigationLink("SSH 키 관리") { KeyImportScreen() }
            }
        }
        .navigationTitle("설정")
        .onChange(of: serverURL) { _, newValue in APIClient.shared.setBaseURL(newValue) }
    }
}
```
> `APIClient.shared`가 없으면 `APIClient`의 공유 인스턴스 접근 방식에 맞춰 조정(예: 앱이 보유한 인스턴스 주입). 구현 시 실제 접근자를 확인해 배선.

- [ ] **Step 3: DeviceListScreen** — `Hydra/HydraiOS/Screens/DeviceListScreen.swift`

```swift
import SwiftUI

struct DeviceListScreen: View {
    let onSelect: (Device) -> Void
    @State private var devices: [Device] = []
    @State private var error: String?
    @State private var loading = false

    var body: some View {
        List {
            if let error {
                Section { Text(error).foregroundStyle(.red) }
            }
            ForEach(devices) { device in
                Button { onSelect(device) } label: {
                    HStack {
                        VStack(alignment: .leading) {
                            Text(device.displayName).font(.headline)
                            Text(device.tailscaleIp).font(.caption).foregroundStyle(.secondary)
                        }
                        Spacer()
                        if device.sshEnabled { Image(systemName: "terminal") }
                    }
                }
                .disabled(!device.sshEnabled)
            }
        }
        .overlay { if loading { ProgressView() } }
        .navigationTitle("디바이스")
        .refreshable { await load() }
        .task { await load() }
    }

    private func load() async {
        loading = true; defer { loading = false }
        do { devices = try await APIClient.shared.listDevices(); error = nil }
        catch { self.error = "목록 조회 실패: \(error.localizedDescription)" }
    }
}
```
> `Device`가 `Identifiable`인지 확인(아니면 `id: \.id`). `displayName`은 mac 코드에서 쓰이므로 존재. `APIClient` 접근자는 Step 2 주석대로 실제에 맞춤.

- [ ] **Step 4: iOS 빌드 확인**

Run: `cd /Users/dave/iWorks/hydra/Hydra && xcodegen generate >/dev/null && xcodebuild -project Hydra.xcodeproj -scheme HydraiOS -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -10`
Expected: `** BUILD SUCCEEDED **`. (RootView가 아직 이들을 안 쓰면 미사용 경고는 무방 — Task 4에서 배선.)
- 컴파일 에러(예: `APIClient.shared` 없음, `Device.displayName`/`Identifiable` 불일치)는 실제 타입에 맞춰 조정하고 이유를 리포트에 기록.

- [ ] **Step 5: 커밋**

```bash
cd /Users/dave/iWorks/hydra
git add Hydra/HydraiOS/Screens/
git commit -m "feat(ios): settings, SSH key import, and device list screens"
```

---

### Task 3: iOS 터미널 뷰 + 화면 (mac 미러링 + TOFU)

**Files:**
- Create: `Hydra/HydraiOS/Terminal/SwiftTermRepresentableiOS.swift`, `Hydra/HydraiOS/Terminal/TerminalScreen.swift`

**Interfaces:**
- Consumes: `TerminalSession`, `TerminalSessionStore`, SwiftTerm iOS `TerminalView`/`TerminalViewDelegate`.

- [ ] **Step 1: SwiftTermRepresentableiOS** — mac `SwiftTermRepresentable`(NSViewRepresentable) 미러링

`Hydra/HydraiOS/Terminal/SwiftTermRepresentableiOS.swift`:
```swift
import SwiftUI
import SwiftTerm

/// iOS mirror of the macOS SwiftTermRepresentable. Wraps SwiftTerm's UIKit
/// `TerminalView`: session output feeds the view; user input/resize flow back
/// to the SSH session via the delegate.
struct SwiftTermRepresentableiOS: UIViewRepresentable {
    let session: TerminalSession

    func makeCoordinator() -> Coordinator { Coordinator(session: session) }

    func makeUIView(context: Context) -> SwiftTerm.TerminalView {
        let view = SwiftTerm.TerminalView(frame: CGRect(x: 0, y: 0, width: 640, height: 400))
        view.terminalDelegate = context.coordinator
        session.onOutput = { [weak view] data in
            guard let view else { return }
            view.feed(byteArray: [UInt8](data)[...])
        }
        return view
    }

    func updateUIView(_ uiView: SwiftTerm.TerminalView, context: Context) {}

    final class Coordinator: NSObject, TerminalViewDelegate {
        let session: TerminalSession
        init(session: TerminalSession) { self.session = session }

        func send(source: SwiftTerm.TerminalView, data: ArraySlice<UInt8>) {
            MainActor.assumeIsolated { session.send(Data(data)) }
        }
        func sizeChanged(source: SwiftTerm.TerminalView, newCols: Int, newRows: Int) {
            MainActor.assumeIsolated { session.resize(cols: newCols, rows: newRows) }
        }
        func setTerminalTitle(source: SwiftTerm.TerminalView, title: String) {}
        func hostCurrentDirectoryUpdate(source: SwiftTerm.TerminalView, directory: String?) {}
        func scrolled(source: SwiftTerm.TerminalView, position: Double) {}
        func clipboardCopy(source: SwiftTerm.TerminalView, content: Data) {}
        func rangeChanged(source: SwiftTerm.TerminalView, startY: Int, endY: Int) {}
        func requestOpenLink(source: SwiftTerm.TerminalView, link: String, params: [String: String]) {}
        func bell(source: SwiftTerm.TerminalView) {}
        func iTermContent(source: SwiftTerm.TerminalView, content: ArraySlice<UInt8>) {}
    }
}
```
> SwiftTerm iOS `TerminalViewDelegate`의 정확한 필수 메서드 집합은 mac과 다를 수 있다(iOS는 `requestOpenLink`/`bell`/`iTermContent` 추가). 컴파일 에러가 나면 프로토콜 요구사항에 맞춰 빈 구현을 추가/제거한다(로직 없는 빈 스텁은 델리게이트 준수로 정당).

- [ ] **Step 2: TerminalScreen** — 연결·TOFU·뷰

`Hydra/HydraiOS/Terminal/TerminalScreen.swift`:
```swift
import SwiftUI
import SSHTransport

struct TerminalScreen: View {
    let device: Device
    @StateObject private var session: TerminalSession
    @State private var trustSHA: String?

    init(device: Device) {
        self.device = device
        _session = StateObject(wrappedValue: TerminalSession(device: device,
            sessionFactory: { TerminalSessionStore.defaultBackend() }))
    }

    var body: some View {
        SwiftTermRepresentableiOS(session: session)
            .ignoresSafeArea(.container, edges: .bottom)
            .navigationTitle(device.displayName)
            .navigationBarTitleDisplayMode(.inline)
            .task { await session.connect(cols: 80, rows: 24) }
            .onDisappear { session.close() }
            .onChange(of: hostKeyPromptSHA) { _, sha in trustSHA = sha }
            .alert("호스트 키 신뢰?", isPresented: Binding(
                get: { trustSHA != nil }, set: { if !$0 { trustSHA = nil } })) {
                Button("신뢰") { Task { await session.trustPendingHostKey() }; trustSHA = nil }
                Button("취소", role: .cancel) { session.cancelPendingHostKey(); trustSHA = nil }
            } message: {
                Text("SHA256:\n\(trustSHA ?? "")")
            }
            .overlay(alignment: .bottom) { statusBar }
    }

    private var hostKeyPromptSHA: String? {
        if case .needsTrust(let sha) = session.hostKeyPrompt { return sha }
        return nil
    }

    @ViewBuilder private var statusBar: some View {
        if case .disconnected(let reason) = session.state, let reason {
            Text(reason).font(.caption).padding(6)
                .background(.ultraThinMaterial).foregroundStyle(.red)
        }
    }
}
```
> `TerminalSession`이 `@MainActor ObservableObject`이므로 `@StateObject`로 보유하고 `hostKeyPrompt`/`state`(@Published) 관찰. `HostKeyDecision`이 `SSHTransport`가 아니라 앱 타입이면 import 조정. `.onChange(of: hostKeyPromptSHA)`가 Optional 비교 가능해야 함(String? Equatable OK).

- [ ] **Step 3: iOS 빌드 확인**

Run: `cd /Users/dave/iWorks/hydra/Hydra && xcodegen generate >/dev/null && xcodebuild -project Hydra.xcodeproj -scheme HydraiOS -destination 'generic/platform=iOS Simulator' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -30`
Expected: `** BUILD SUCCEEDED **`. TerminalViewDelegate 요구 메서드 불일치 에러는 Step 1 주석대로 맞춘다.

- [ ] **Step 4: 커밋**

```bash
cd /Users/dave/iWorks/hydra
git add Hydra/HydraiOS/Terminal/
git commit -m "feat(ios): SwiftTerm iOS terminal view + connect/TOFU screen

UIViewRepresentable mirroring the macOS SwiftTermRepresentable, wired to the
shared TerminalSession (Citadel backend); host-key TOFU surfaced as an alert."
```

---

### Task 4: RootView 통합 + iOS 빌드·부팅 + mac 회귀 + 문서

**Files:**
- Create: `Hydra/HydraiOS/RootView.swift`
- Modify: `Hydra/HydraiOS/App.swift`; Delete: `Hydra/HydraiOS/PlaceholderView.swift`
- Modify: `Hydra/HydraiOS/README.md`

- [ ] **Step 1: RootView** — `Hydra/HydraiOS/RootView.swift`

```swift
import SwiftUI

struct RootView: View {
    @State private var selected: Device?

    var body: some View {
        TabView {
            NavigationStack {
                DeviceListScreen(onSelect: { selected = $0 })
            }
            .tabItem { Label("디바이스", systemImage: "server.rack") }

            NavigationStack { SettingsScreen() }
                .tabItem { Label("설정", systemImage: "gear") }
        }
        // fullScreenCover requires only Identifiable (Device is Codable+Identifiable),
        // avoiding a Hashable conformance on the shared Device model. The terminal
        // gets its own NavigationStack for the title/Done button.
        .fullScreenCover(item: $selected) { device in
            NavigationStack {
                TerminalScreen(device: device)
                    .toolbar { ToolbarItem(placement: .cancellationAction) {
                        Button("닫기") { selected = nil }
                    } }
            }
        }
    }
}
```
> `Device: Identifiable`(확인됨: `id: String`)이라 `.fullScreenCover(item:)`가 안전. 공유 `Device` 모델에 `Hashable`을 추가하지 않는다(mac 회귀 회피).

- [ ] **Step 2: App.swift에서 RootView로 교체 + PlaceholderView 제거**

`Hydra/HydraiOS/App.swift`의 `WindowGroup { PlaceholderView() }` → `WindowGroup { RootView() }`. 그다음 `git rm Hydra/HydraiOS/PlaceholderView.swift`.

- [ ] **Step 3: iOS Simulator 빌드 + 부팅**

```bash
cd /Users/dave/iWorks/hydra/Hydra
xcodegen generate >/dev/null
xcodebuild -project Hydra.xcodeproj -scheme HydraiOS -destination 'generic/platform=iOS Simulator' -derivedDataPath build/ios build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -8
DEV=$(xcrun simctl list devices available | grep -m1 "iPad" | grep -oE "\([0-9A-F-]{36}\)" | tr -d "()")
APP=$(find build/ios -name "HydraiOS.app" -type d | head -1)
xcrun simctl boot "$DEV" 2>/dev/null || true
xcrun simctl install "$DEV" "$APP" && xcrun simctl launch "$DEV" com.hydra.ios && echo "LAUNCH OK"
xcrun simctl io "$DEV" screenshot /tmp/hydraios-b2b.png && echo "screenshot saved"
```
Expected: `** BUILD SUCCEEDED **` + `LAUNCH OK`. 스크린샷에 디바이스/설정 탭이 보임. 시뮬레이터 이름/OS는 존재하는 것으로 맞춘다.

- [ ] **Step 4: macOS 회귀**

Run: `cd /Users/dave/iWorks/hydra/Hydra && swift test 2>&1 | tail -4 && cd /Users/dave/iWorks/hydra && make hydra-app 2>&1 | tail -4`
Expected: 79 tests/1 skip/0 fail + macOS `.app` 빌드 성공.

- [ ] **Step 5: README 갱신** — `Hydra/HydraiOS/README.md`에 B2b 사용법 섹션 추가:

```markdown
## Using it (B2b)
1. Settings tab → set server URL (`http://<mac-LAN-IP>:8080`) and SSH username.
2. Settings → SSH 키 관리 → paste your ed25519 private key (or import from Files) → 저장.
3. Devices tab → tap an SSH-enabled node → trust the host key → shell.

Real device install requires signing (automatic signing + your Apple ID team in
Xcode; free personal team re-signs weekly). Citadel needs your ed25519 key
authorized on the target node.
```

- [ ] **Step 6: 커밋**

```bash
cd /Users/dave/iWorks/hydra
git add Hydra/HydraiOS/RootView.swift Hydra/HydraiOS/App.swift Hydra/HydraiOS/README.md
git rm Hydra/HydraiOS/PlaceholderView.swift
git commit -m "feat(ios): wire RootView (devices + settings + terminal); B2b MVP complete"
```

---

## Self-Review 체크
- **스펙 커버리지**: 자격(T1) / 설정·키임포트·디바이스목록(T2) / 터미널뷰+TOFU(T3) / RootView 통합·부팅·회귀·문서(T4) — 스펙 §2 전부 커버. Bonjour/키생성/탭/스크롤백은 스펙대로 이후.
- **Placeholder**: 없음. 각 화면 완전 코드 + iOS 빌드 게이트. 실제 타입 적합성(`APIClient.shared`/`Device: Identifiable/Hashable`/`TerminalViewDelegate` 요구 메서드)은 컴파일로 확정하도록 명시(플랜의 주석 = 구현 시 실제 확인 지점).
- **타입 일관성**: `makeImportedCredentials`/`SSHCredentials`/`ResolvedKey`(A) ↔ T1 사용 일치. `TerminalSession`(@MainActor ObservableObject, `send/resize/close/connect/trustPendingHostKey/cancelPendingHostKey/hostKeyPrompt/state/onOutput`)은 B2a 기존 API. `SwiftTerm.TerminalView.feed(byteArray:)`/`terminalDelegate`는 확인됨.
- **리스크**: T3 SwiftTerm iOS 델리게이트 요구 메서드 집합 + `Device` 프로토콜 적합성이 컴파일 조정 지점(플랜에 명시).

## Execution Handoff
계획 저장 완료. 사용자 글로벌 기본값([[plan-execution-default]])에 따라 **subagent-driven-development**로 바로 실행한다.
